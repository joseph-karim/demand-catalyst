---
import WatchlistMockup from './mockups/WatchlistMockup.astro';
import QueueMockup from './mockups/QueueMockup.astro';
import BriefMockup from './mockups/BriefMockup.astro';
import OutreachMockup from './mockups/OutreachMockup.astro';
import SyncMockup from './mockups/SyncMockup.astro';
import SearchMockup from './mockups/SearchMockup.astro';
---

<section class="features" id="features">
    <div class="container">
        <div class="section-label">The platform</div>
        <h2>Tools that replace the research, not the relationship.</h2>
        <p class="features-subtitle">Everything a producer needs to act on a signal. Nothing extra to learn.</p>

        <div class="feature-list">
            <div class="feature-row">
                <div class="feature-text reveal">
                    <div class="feature-icon">
                        <svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round">
                            <circle cx="12" cy="12" r="10" /><circle cx="12" cy="12" r="6" /><circle cx="12" cy="12" r="2" />
                            <line x1="12" y1="2" x2="12" y2="4" /><line x1="12" y1="20" x2="12" y2="22" />
                            <line x1="2" y1="12" x2="4" y2="12" /><line x1="20" y1="12" x2="22" y2="12" />
                        </svg>
                    </div>
                    <h4>Prospect Watchlist</h4>
                    <p>Add the contractors you want to write. We tell you the moment they need new coverage.</p>
                </div>
                <div class="feature-visual">
                    <WatchlistMockup />
                </div>
            </div>

            <div class="feature-row feature-row-reverse">
                <div class="feature-text reveal">
                    <div class="feature-icon">
                        <svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round">
                            <line x1="8" y1="6" x2="21" y2="6" /><line x1="8" y1="12" x2="21" y2="12" /><line x1="8" y1="18" x2="21" y2="18" />
                            <rect x="3" y="4" width="2" height="2" rx="0.5" fill="currentColor" stroke="none" />
                            <rect x="3" y="10" width="2" height="2" rx="0.5" fill="currentColor" stroke="none" />
                            <rect x="3" y="16" width="2" height="2" rx="0.5" fill="currentColor" stroke="none" />
                        </svg>
                    </div>
                    <h4>Daily Queue</h4>
                    <p>One list, sorted by who to call first. See what changed and why it matters.</p>
                </div>
                <div class="feature-visual">
                    <QueueMockup />
                </div>
            </div>

            <div class="feature-row">
                <div class="feature-text reveal">
                    <div class="feature-icon">
                        <svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round">
                            <path d="M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8z" />
                            <polyline points="14 2 14 8 20 8" />
                            <line x1="16" y1="13" x2="8" y2="13" /><line x1="16" y1="17" x2="8" y2="17" />
                        </svg>
                    </div>
                    <h4>Account Brief</h4>
                    <p>Signals, gaps, and talking points pulled together in seconds. Every source linked so you can check it yourself.</p>
                </div>
                <div class="feature-visual">
                    <BriefMockup />
                </div>
            </div>

            <div class="feature-row feature-row-reverse">
                <div class="feature-text reveal">
                    <div class="feature-icon">
                        <svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round">
                            <line x1="22" y1="2" x2="11" y2="13" />
                            <polygon points="22 2 15 22 11 13 2 9 22 2" />
                        </svg>
                    </div>
                    <h4>Ready-to-Send Outreach</h4>
                    <p>Messages that mention their real project or hire. Not a cold pitch, a warm intro.</p>
                </div>
                <div class="feature-visual">
                    <OutreachMockup />
                </div>
            </div>

            <div class="feature-row">
                <div class="feature-text reveal">
                    <div class="feature-icon">
                        <svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round">
                            <polyline points="23 4 23 10 17 10" /><polyline points="1 20 1 14 7 14" />
                            <path d="M3.51 9a9 9 0 0 1 14.85-3.36L23 10" />
                            <path d="M20.49 15a9 9 0 0 1-14.85 3.36L1 14" />
                        </svg>
                    </div>
                    <h4>AMS Write-Back</h4>
                    <p>Every email and call logs to your system automatically. No double entry.</p>
                </div>
                <div class="feature-visual">
                    <SyncMockup />
                </div>
            </div>

            <div class="feature-row feature-row-reverse">
                <div class="feature-text reveal">
                    <div class="feature-icon">
                        <svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round">
                            <circle cx="11" cy="11" r="8" /><line x1="21" y1="21" x2="16.65" y2="16.65" />
                            <line x1="8" y1="11" x2="14" y2="11" /><line x1="11" y1="8" x2="11" y2="14" />
                        </svg>
                    </div>
                    <h4>Instant Search</h4>
                    <p>Type any business name. Get their signals, permits, and hiring data in seconds.</p>
                </div>
                <div class="feature-visual">
                    <SearchMockup />
                </div>
            </div>
        </div>
    </div>
</section>

<style>
    .features {
        padding: 120px 40px;
        background: var(--color-paper);
    }

    .features h2 {
        font-family: var(--font-serif);
        font-size: clamp(28px, 3vw, 40px);
        font-weight: 700;
        line-height: 1.2;
        letter-spacing: -0.02em;
        margin-bottom: 16px;
    }

    .features-subtitle {
        font-size: 17px;
        color: var(--color-slate);
        margin-bottom: 72px;
        max-width: 520px;
    }

    .feature-list {
        display: flex;
        flex-direction: column;
        gap: 80px;
    }

    .feature-row {
        display: grid;
        grid-template-columns: 1fr 1fr;
        gap: 64px;
        align-items: center;
    }

    .feature-row-reverse {
        direction: rtl;
    }

    .feature-row-reverse > * {
        direction: ltr;
    }

    .feature-text {
        max-width: 400px;
    }

    .feature-icon {
        width: 48px;
        height: 48px;
        background: var(--color-warm);
        border-radius: 12px;
        display: flex;
        align-items: center;
        justify-content: center;
        margin-bottom: 20px;
        color: var(--color-ink);
    }

    .feature-text h4 {
        font-size: 22px;
        font-weight: 700;
        margin-bottom: 10px;
        letter-spacing: -0.01em;
    }

    .feature-text p {
        font-size: 16px;
        color: var(--color-slate);
        line-height: 1.65;
    }

    .feature-visual {
        display: flex;
        justify-content: center;
    }

    @media (max-width: 900px) {
        .features {
            padding: 80px 24px;
        }

        .feature-list {
            gap: 56px;
        }

        .feature-row,
        .feature-row-reverse {
            grid-template-columns: 1fr;
            gap: 32px;
            direction: ltr;
        }

        .feature-text {
            max-width: 100%;
        }

        .feature-visual {
            justify-content: flex-start;
        }
    }
</style>
